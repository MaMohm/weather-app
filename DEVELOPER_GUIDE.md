# 👨‍💻 Developer Guide: Weather App Internals

هذا الدليل مخصص لك (المطور) لفهم "ما تحت الغطاء" وكيفية تطوير المشروع مستقبلاً.

## 🏗️ هيكلية المشروع (Project Architecture)

### 1. Frontend (`/frontend`)
مبني بـ **React + Vite**.
- **القلب النابض:** `src/pages/Home.jsx`. هنا تجد كل المنطق:
  - `handleLocationClick`: دالة جلب الموقع الجغرافي.
  - `renderMiniCard`: دالة رسم الكروت الجانبية.
  - `useEffect`: الذي يقوم بتحميل بيانات لندن/وباريس عند فتح الموقع.
- **التصميم:** `src/index.css`. قمت بإضافة متغيرات CSS (Variables) للألوان والخطوط.
- **الخدمات:** `src/services/api.js`. هذا الملف هو "الوسيط" الذي يتحدث مع السيرفر الخلفي.
- **المساعدة:** `src/utils/weatherUtils.js`. يحتوي على دالة `getWeatherImage` التي تختار الصورة بناءً على كود الطقس (مثلاً 0 = سماء صافية).

### 2. Backend (`/backend`)
مبني بـ **Node.js + Express + TypeScript**.
- **نقطة الدخول:** `src/index.ts`.
  - ستجد هنا `app.get('/api/weather', ...)` وهو الـ Endpoint الرئيسي.
  - **Rate Limiting:** ابحث عن `const limiter`. يمكنك تغيير الرقم `300` لزيادة أو تقليل الحد المسموح.
  - **CORS:** ابحث عن `allowedOrigins` لإضافة روابط جديدة إذا قمت برفع الموقع على دومين جديد.

---

## 🚀 كيف تضيف ميزة جديدة؟ (سيناريوهات شائعة)

### سيناريو 1: تغيير الصور الخلفية
1. اذهب إلى `frontend/src/utils/weatherUtils.js`.
2. ابحث عن دالة `getWeatherImage`.
3. ستجد روابط Unsplash. قم باستبدال الرابط برابط صورتك الجديدة.

### سيناريو 2: إضافة مدينة جديدة إلى القائمة المقترحة
1. اذهب إلى `frontend/src/pages/Home.jsx`.
2. ابحث عن مصفوفة `POPULAR_CITIES` في الأعلى.
3. أضف سطرًا جديدًا: `{ name: 'Riyadh', country: 'Saudi Arabia', latitude: 24.7136, longitude: 46.6753, timezone: 'Asia/Riyadh' },`.

### سيناريو 3: جلب بيانات إضافية (مثل الضغط الجوي)
1. **Backend:** عدّل `backend/src/index.ts`، وابحث عن `current: 'temperature_2m,...'`. أضف المتغير الذي تريده من وثائق Open-Meteo.
2. **Frontend:** اذهب إلى `Home.jsx` واستقبل البيانات الجديدة عبر `weatherData.current.pressure_msl`.

---

## 🛡️ نصائح أمنية للمستقبل
- **لا ترفع ملف `.env` أبداً.**
- إذا أردت إضافة "تسجيل دخول"، يجب أن تضيف قاعدة بيانات (MongoDB أو PostgreSQL) لأن المشروع الحالي لا يستخدم قاعدة بيانات.

---
*احتفظ بهذا الدليل كمرجع لك.*
